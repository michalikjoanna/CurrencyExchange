package com.kainoss.exchange.converters;

import com.kainoss.exchange.model.TimeSeriesFX;

import java.net.URL;

public class TimeSeriesFXConverter  extends JsonConverter<TimeSeriesFX> {
    public TimeSeriesFXConverter(URL jsonURL) {
        super(jsonURL);
    }
}
